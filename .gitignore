# Ignore all log and temp files anywhere in the folder
**/*.log
**/*.tmp

# Ignore all shaders regardless of which subfolder they are in
**/shaders/*.glsl

# Optional: Ignore the swap files created by Vim/NeoVim if you use them
*.swp

# Ignore the backup script itself (optional, but keeps the repo clean)
scripts/hypr-backup.sh

# Ignore git locks
*.lock
.git/
